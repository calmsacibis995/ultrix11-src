If you have to change the version number on n1.c from 1123 to any number,
remember to redo the installation for everything under /usr/src/cmd/troff,
otherwise you might run into the message "wrong version" at some point.
